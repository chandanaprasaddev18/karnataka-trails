# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Env — .env.example is committed, .env is not
.env
.env.local

# Downloaded photographs. Generated by `tripplan fetch-photos` — reproducible
# from the database, ~9MB, and not source. Same reasoning as not committing a
# built asset bundle.
web/public/photos/

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Geocoder response cache written by api/tools/import_directory.py. Regenerated on
# demand; the reviewed OUTPUT (seed YAML + geocode_aliases.yaml) is what we keep.
api/seeds/.geocode_cache.json

# Next.js build output
web/.next/
web/out/
